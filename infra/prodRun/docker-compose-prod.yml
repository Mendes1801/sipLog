version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: sip_postgres
    environment:
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    ports:
      - "5432:5432"
    restart: unless-stopped
    volumes:
      - ./volume/postgres:/var/lib/postgresql/data

  core-api:
    build:
      context: ../../core_api/apiCore-sipLog
      dockerfile: Dockerfile
    container_name: siplog-core-api
    ports:
      - "8081:8081"
    depends_on:
      - postgres
    environment:
      - JAVA_OPTS=${JAVA_OPTS}
      - SPRING_PROFILES_ACTIVE=${SPRING_PROFILES_ACTIVE}
      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/${DB_NAME}
      - SPRING_DATASOURCE_USERNAME=${DB_USER}
      - SPRING_DATASOURCE_PASSWORD=${DB_PASSWORD}
      - KEYCLOAK_SERVER_BASE_URL=${KC_BACKEND_URL}
      - KEYCLOAK_SERVER_URL=${KC_BACKEND_URL}/realms/${KC_REALM}
      # Nota: Se em produção você também tiver o Keycloak containerizado, adicione aqui. Se for externo, use o IP.

  bff:
    build:
      context: ../../sipLog-BFF
      dockerfile: Dockerfile
    container_name: siplog-bff
    ports:
      - "8080:8080"
    depends_on:
      - core-api
    environment:
      - JAVA_OPTS=${JAVA_OPTS}
      - SPRING_PROFILES_ACTIVE=${SPRING_PROFILES_ACTIVE}
      - CORE_API_URL=${CORE_API_URL}
      - KEYCLOAK_SERVER_BASE_URL=${KC_BACKEND_URL}
      - KEYCLOAK_SERVER_URL=${KC_BACKEND_URL}/realms/${KC_REALM}
